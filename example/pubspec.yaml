name: example
description: "Example"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  module:
    git: https://github.com/JonathanVegasP/module.git
  logging: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  module_builder:
    git: https://github.com/JonathanVegasP/module_builder.git
  build_runner: ^2.4.15

flutter:
  uses-material-design: true
